import 'package:flutter/material.dart';

void main(){
  runApp(Calculator());
}

class Calculator extends StatelessElement{
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}